<h1>New Playlist song</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.playlist_song_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.playlist_song_path(@conn, :index) %></span>
